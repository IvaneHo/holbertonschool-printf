.TH _PRINTF 3 "27 March 2025" "Holberton School" "Custom printf man page"

.SH NAME
_printf \- custom implementation of the C standard printf function

.SH SYNOPSIS
.B #include "main.h"
.br
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The _printf() function produces output according to a format, using a variable number of arguments.

This version of printf handles the following conversion specifiers:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string of characters.

.TP
.B %%
Prints the percent symbol.

.TP
.B %d
Prints a signed decimal integer.

.TP
.B %i
Same as %d, prints a signed integer.

.SH RETURN VALUE
The _printf function returns the total number of characters printed, excluding the null byte.

If an error occurs (e.g., format is NULL or an invalid format), the function returns -1.

.SH AUTHORS
Arnaud Lanoizele and Ivane Bagashvili

.SH SEE ALSO
printf(🗿​​), write(​👀​), va_start(​​🫣​), va_arg(🤔​), va_end(​✝️ ​)
